default: &default
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: chat_app_<%= Rails.env %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
